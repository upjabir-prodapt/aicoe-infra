trigger:
- main
pool:
  vmImage: 'ubuntu-latest' # Uses Microsoft's free hosted servers
variables:
  GCP_PROJECT_ID: 'aicoe-sharedwif'
  GCP_PROJECT_NUMBER: '538669417284' # 12-digit number
  GCP_SERVICE_ACCOUNT: 'aicoe-shared-wip-sa-tf@aicoe-sharedwif.iam.gserviceaccount.com'
  ADO_SERVICE_CONNECTION_NAME: 'sc-ado-gcp-aicoe' # Exact name of your Service Connection
  WIF_POOL_NAME: 'aicoe-ado-wip'
  WIF_PROVIDER_NAME: 'ado-gcp-wif'
steps:
- task: AzureCLI@2
  displayName: 'Passwordless WIF Authentication to GCP'
  inputs:
    azureSubscription: $(ADO_SERVICE_CONNECTION_NAME)
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    addSpnToEnvironment: true
    inlineScript: |
      echo "Starting passwordless authentication to GCP..."
 
      # 1. Save the ADO OIDC token to a temporary file
      echo $idToken > $(Pipeline.Workspace)/.workload_identity.jwt
 
      # 2. Create output directory FIRST (this was missing!)
      mkdir -p $(Build.ArtifactStagingDirectory)/wif-debug
 
      # 3. Decode JWT payload correctly
      JWT=$(cat $(Pipeline.Workspace)/.workload_identity.jwt)
      PAYLOAD=$(echo $JWT | cut -d'.' -f2)
 
      # 4. Pad base64 and pipe directly to file (no || logic)
      echo "${PAYLOAD}==" \
        | base64 --decode \
        | python3 -m json.tool > $(Build.ArtifactStagingDirectory)/wif-debug/jwt-payload.json
 
      # 5. Verify file was created
      echo "=== File check ==="
      ls -lrt $(Build.ArtifactStagingDirectory)/wif-debug/
      echo "=== JWT Payload (from file) ==="
      cat $(Build.ArtifactStagingDirectory)/wif-debug/jwt-payload.json
 
- task: PublishBuildArtifacts@1
  displayName: 'Publish WIF Debug Artifacts'
  condition: always()
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)/wif-debug'
    ArtifactName: 'wif-debug'
    publishLocation: 'Container'