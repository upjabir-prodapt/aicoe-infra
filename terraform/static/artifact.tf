# resource "google_artifact_registry_repository" "aicoe_artifact_repo" {
    
# project       = "${var.project}${var.envname}"
# location      = var.region
# repository_id = "${var.project}${var.envname}-${var.artifact_format}-repo"
# format        = var.artifact_format

#  labels = {
#     env    = var.envname
#     system = "${var.project}${var.envname}"
#   }
# docker_config  {
#   immutable_tags = true
# }
# }