# State migration from pre-module layout to modular layout (om-focuslane-dev).
# Nothing to move here - google_firestore_database.om_focus_lane_firestore
# keeps its original resource address (no shared module owns it yet, see
# main.tf note), and this layer had no other resources.
